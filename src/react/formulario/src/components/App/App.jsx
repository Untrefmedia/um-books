import React from 'react';
import Calendar from '../Calendar/Calendar';
import Form from '../Form/Form';

const App = (props) => {
	return (
		<div>
			<Form />
			<Calendar />
		</div>
	);
};

export default App;
